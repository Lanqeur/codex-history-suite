interface:
  display_name: "Codex History"
  short_description: "Search and trace local Codex history with evidence"
  default_prompt: "Use $codex-history to retrieve and trace relevant decisions from my local Codex history."
policy:
  allow_implicit_invocation: true
